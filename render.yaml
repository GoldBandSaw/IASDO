services:
  - type: web
    name: campusflow
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: COMMON_CALENDAR_URL
        sync: false
